# 历史遗留清理

这份文档只记录现在的判断。旧方案不再和新方案并列描述，避免后续误读。

## 1. 当前结论

按现在的代码状态，历史遗留分三类：

1. 已经清理完成的旧旁路能力
2. 需要继续保留的主流程或运维能力
3. 只改文档、不动代码的内容

主要结论：

- 团队任务卡片回写已经是正式功能，和单人卡片一样需要持久化
- 生成调试面板已由 `miniprogram/utils/config.js` 的 `debugGenerationPanel` 控制；当前联调配置为打开，上线前建议按开发版 / 体验版 / 正式版配置收口
- AI 核验链路已下线
- 贴纸链路已删除，`generateCompanionNote` 已拆成独立云函数和 `services/companion.js`

---

## 2. 已清理完成的旧旁路能力

以下内容已经不存在，或已经保持删除状态。后续不要再当成待办重复追踪。

1. `tmp/` 下的高德 POI 临时包和解压文件  
   当前仓库未保留 `tmp/` 目录。

2. 公共漫步列表
   现在没有 `listPublicWalks` 云函数，也没有公共广场页入口。这条线保持删除。

3. 团队动态独立接口
   现在没有 `listTeamActivities` 云函数。团队详情页已经直接返回动态信息，不需要单独接口。

4. 本地直连修复脚本
   现在没有 `scripts/repair_dissolved_team_rooms.js`。历史 `dissolved` 数据统一用 `repairDissolvedTeamRooms` 云函数处理。

5. 素材生成/下载脚本
   现在没有 `download_assets.js`、`generate_loading_cat_gif.py`、`generate_sound_achievement.py`。素材生成脚本不属于主流程。

---

## 3. 已转为正式链路的能力

1. 团队任务卡片持久化
   文件：[saveTeamMissionCard](D:/liuliu-minimap/cloudfunctions/saveTeamMissionCard/index.js:1)
   这项不能删除。团队结果页通过 `saveTeamMissionCard` 把卡片路径写回房间文档，和单人 `missionAssetMap.cardImagePath` 一样，都是“首次生成、后续复用”。

---

## 4. 需要裁剪的功能

1. 生成调试面板
   文件：[index.wxml](D:/liuliu-minimap/miniprogram/pages/index/index.wxml:253)、[index.js](D:/liuliu-minimap/miniprogram/pages/index/index.js:1948)  
   当前已受 `debugGenerationPanel` 控制。本次为排查生成阶段和地点上下文，联调配置保持打开，并新增“地点原始信息”用于查看高德逆地理、AOI、商圈、POI、道路和地址组件。剩余建议是把该开关进一步接入开发版 / 体验版 / 正式版配置，而不是继续把它视为默认暴露问题。

2. AI 任务核验链路
   这条流程已经删除，不再属于主流程。

3. 旧 AI 贴纸分支
   贴纸分支已经删除。`generateCompanionNote` 现在是独立云函数，前端统一通过 `services/companion.js` 调用。

---

## 5. 谨慎保留的运维兼容项

1. `repairDissolvedTeamRooms` 云函数  
   文件：[repairDissolvedTeamRooms](D:/liuliu-minimap/cloudfunctions/repairDissolvedTeamRooms/index.js:1)  
   这不是页面功能，而是旧数据修复工具。等旧数据处理完成后，再决定是否删除。

2. `team-room` 对 `dissolved` 状态的兼容展示  
   文件：[team-room.js](D:/liuliu-minimap/miniprogram/pages/team-room/team-room.js:20)  
   旧数据里仍可能出现 `dissolved`，暂时保留兼容。

3. RAG 相关文档描述  
   README 和云函数总览已按共享运行时更新，不再把旧 RAG 运行时当作必配项。这类问题只改文档，不影响代码。

---

## 6. 不要误删的主链路

以下内容仍在主流程里，清理历史遗留时不要误删：

- `publishWalkShare` / `isPublic` 仍服务“已结束单人记录分享给微信朋友”
- `checkin-card` 仍服务打卡卡片的展示和保存
- `generateCompanionNote` 已作为独立能力接回当前打卡卡片链路
- 同行相关页面和房间主链路仍然在产品范围内

---

## 7. 结论

后续按这个顺序处理：

1. 已删除的旧旁路继续保持删除，不恢复公共广场、独立团队动态、旧贴纸或 AI 核验
2. 团队卡片持久化、分享、成就重算、同行主流程按正式功能维护
3. 下一步可把生成调试面板开关细化到开发版 / 体验版 / 正式版配置，并继续避免“地图中心设点”等旧入口回到主流程
4. 旧数据处理完成后，再评估 `repairDissolvedTeamRooms` 和 `dissolved` 兼容展示是否可以归档
