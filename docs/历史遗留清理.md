# 历史遗留清理

这份文档只记录当前最新判断，不再保留旧口径和新口径并列的写法。

## 1. 当前结论

按最新规则，历史遗留可以分成三类：

1. 可以继续删除的旧旁路能力
2. 需要改造后再决定是否保留的能力
3. 只需要更新文档口径、暂时不动代码的能力

当前最重要的变化是：

- 团队任务卡片回写不再按“可删项”处理，而是要改成和单人卡片同样的持久化链路
- 调试面板要做成选择性展示，不再默认面向普通用户露出
- AI 核验链路已下线
- 贴纸链路已删除，`generateCompanionNote` 已拆成独立云函数和 `services/companion.js`

---

## 2. 继续删除的旧旁路能力

以下能力仍然属于历史旁路，适合优先清理：

1. `tmp/` 下的高德 POI 临时包和解压文件  
   这批文件不属于主流程，也没有继续保留的业务价值。

2. 公共漫步列表链路  
   文件：[listPublicWalks](D:/liuliu-minimap/cloudfunctions/listPublicWalks/index.js:1)、[walk.js](D:/liuliu-minimap/miniprogram/services/walk.js:11)、[api.js](D:/liuliu-minimap/miniprogram/services/api.js:477)  
   当前没有公共广场页，这条链路可以继续保持删除态。

3. 团队动态独立接口  
   文件：[listTeamActivities](D:/liuliu-minimap/cloudfunctions/listTeamActivities/index.js:1)、[team.js](D:/liuliu-minimap/miniprogram/services/team.js:31)、[api.js](D:/liuliu-minimap/miniprogram/services/api.js:594)  
   团队详情已经内嵌动态信息，这条独立接口不再是主链路。

5. 本地直连修复脚本  
   文件：[repair_dissolved_team_rooms.js](D:/liuliu-minimap/scripts/repair_dissolved_team_rooms.js:1)  
   这类一次性运维脚本适合从主仓库移除。

6. 素材生成/下载脚本  
   文件：[download_assets.js](D:/liuliu-minimap/scripts/download_assets.js:1)、[generate_loading_cat_gif.py](D:/liuliu-minimap/scripts/generate_loading_cat_gif.py:1)、[generate_sound_achievement.py](D:/liuliu-minimap/scripts/generate_sound_achievement.py:1)  
   这些脚本不参与当前主流程，适合归档后删除。

---

## 3. 需要改造而不是直接删的能力

4. 团队任务卡片持久化  
   文件：[saveTeamMissionCard](D:/liuliu-minimap/cloudfunctions/saveTeamMissionCard/index.js:1)、[api.js](D:/liuliu-minimap/miniprogram/services/api.js:613)  
   这项不再按“可删旁路”处理，而是要改成和单人模式一致的团队卡片持久化链路。  
   也就是说，团队结果卡片不应该只停留在本地渲染，而是要有稳定的回写和读取路径。

---

## 4. 需要裁剪的功能

3. 生成调试面板  
   文件：[index.wxml](D:/liuliu-minimap/miniprogram/pages/index/index.wxml:253)、[index.js](D:/liuliu-minimap/miniprogram/pages/index/index.js:1948)  
   这部分不建议默认暴露给普通用户。最新规则是改成选择性展示：开发态可见，正式态隐藏。

4. AI 任务核验链路  
   文件：[record.js](D:/liuliu-minimap/miniprogram/pages/record/record.js:1439)、[record.js](D:/liuliu-minimap/miniprogram/pages/record/record.js:1480)、[theme.js](D:/liuliu-minimap/miniprogram/services/theme.js:11)、[verifyMission](D:/liuliu-minimap/cloudfunctions/verifyMission/index.js:1)  
   这条链路按最新规则已删除，不再作为主流程的一部分。

5. 旧 AI 贴纸分支  
   文件：[record.js](D:/liuliu-minimap/miniprogram/pages/record/record.js:2126)、[companion.js](D:/liuliu-minimap/miniprogram/services/companion.js:1)、[generateSticker](D:/liuliu-minimap/cloudfunctions/generateSticker/index.js:520)  
   最新规则是删掉贴纸分支，把 `generateCompanionNote` 拆成独立云函数，并把前端服务收敛到 `services/companion.js`，避免它继续和贴纸生成耦在一起。

---

## 5. 谨慎保留的运维兼容项

1. `repairDissolvedTeamRooms` 云函数  
   文件：[repairDissolvedTeamRooms](D:/liuliu-minimap/cloudfunctions/repairDissolvedTeamRooms/index.js:1)  
   这不是主流程，属于一次性修复能力。等旧数据确认处理完成后，再决定是否删除。

2. `team-room` 对 `dissolved` 状态的兼容展示  
   文件：[team-room.js](D:/liuliu-minimap/miniprogram/pages/team-room/team-room.js:20)  
   旧数据还可能出现 `dissolved`，所以这类兼容先不要急着动。

3. RAG 相关文档描述  
   README 和云函数链路总览已经按当前共享运行时口径更新，不再把旧 RAG 运行时当作必配项。  
   这类问题只改文档，不影响主流程代码。

---

## 6. 不要误删的主链路

以下能力仍然是主流程相关，不应因为清理历史遗留而误删：

- `publishWalkShare` / `isPublic` 仍服务“已结束单人记录分享给微信朋友”
- `checkin-card` 仍服务打卡卡片的展示和保存
- `generateCompanionNote` 已作为独立能力接回当前打卡卡片链路
- 同行相关页面和房间主链路仍然在产品范围内

---

## 7. 结论

当前这批遗留不应该再按“能删就删”的思路处理，而要按下面顺序推进：

1. 先清理纯旁路能力
2. 再把团队卡片持久化补齐
3. 再裁剪调试面板、AI 核验和旧贴纸分支
4. 最后再看运维修复和 `dissolved` 兼容项是否还能继续收口
